import 'package:test/test.dart';
import 'package:web3mq_websocket/web3mq_websocket.dart';

void main() {
  group('A group of tests', () {
    final websocoket = Web3MQWebSocketManager(baseUrl: '');

    setUp(() {});

    test('First Test', () {});
  });
}
